# P0-5R Grounding Coverage Deterministic Tests

> **Authority:** `TEST_SCAFFOLD` · `NOT ACCEPTED CANON` · `NOT HUMAN APPROVAL`

New generic coverage test:

```text
TestActivationGroundingCarriesAndCanCiteOffscreenFactSurfaces
```

It proves:

- `offscreen_character_stage` and `initial_state` survive exact activation Grounding projection;
- an unsupported offscreen pressure can be cited at its exact Plan JSON pointer;
- an exact activation decision result can be cited as the source;
- Host finalizes and validates a structured rejection receipt for that path.

The test demonstrates coverage and receipt wiring. Its deterministic verdict is a fake classification and does not claim to measure live-model judgment quality.

Existing regression suites continue to cover:

- P0-4 exact structured verdict and stale receipt binding;
- P0-5 simulation authority and positive-side RAG fields;
- P0-6 Proposal contamination rejection;
- current Plan/activation digest reconstruction.

No paid model was invoked.
