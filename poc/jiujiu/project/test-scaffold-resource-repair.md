# TEST_SCAFFOLD targeted character-resource repair

This is a bounded repair to the isolated TEST_SCAFFOLD foundation. Preserve every existing source Canon fact, character identity, knowledge boundary, relationship TBD, location, goal, pressure, and commitment. Do not change `book_world`, `world_rules`, `world_codex`, `premise`, `layered_outline`, or official Canon.

Target only `characters.json` and only each character's `initial_state.resource_balances`. Keep legacy `resources` empty unless the schema requires otherwise.

Add the minimum ordinary, non-supernatural physical resources needed for the already-authorized World State probe. Every resource is TEST_SCAFFOLD, exists only inside this isolated run, and is not a new character ability or official setting.

Use these exact stable resource identities and consistent world-authoritative fields wherever the same resource is shared:

1. `res_1111222233334444`
   - name: `TEST_SCAFFOLD近岸公开水况`
   - unit: empty string
   - actual_amount: null
   - qualitative shared resource; no readable facts; no inspectable surface required
   - each character may have shared access and an `unknown` perception at chapter 0 under the safe perceived label `近岸公开水况`
   - this resource permits later operational observation; it does not preload any observation or result

2. `res_2222333344445555`
   - name: `TEST_SCAFFOLD现场警示浮标`
   - unit: `件`
   - actual_amount: 1
   - ordinary physical warning object at the nearshore guard point
   - 九九 has shared access, `last_observed` amount 1 at chapter 0, perceived name/label `现场警示浮标`, perceived unit `件`, with a source evidence ref tied to the authored opening resource
   - 九尾狐 and 凤凰 have no access and must not receive its amount as knowledge

3. `res_3333444455556666`
   - name: `TEST_SCAFFOLD空白事件记录纸`
   - unit: `张`
   - actual_amount: 3
   - ordinary blank material with no readable facts
   - 九九 has exclusive access, `last_observed` amount 3 at chapter 0, safe perceived name/label `空白事件记录纸`, perceived unit `张`, with an authored opening evidence ref
   - other characters have no access and do not know the quantity

All perceptions must use the exact v2 `resource_balances` schema, `as_of_chapter: 0`, valid access values, and non-secret evidence refs. Do not encode quantities or secrets in resource IDs. Do not create duplicate world balances. Do not add the fox tide-mark secret to 九九 or 凤凰. Do not alter Phoenix known facts, which remain exactly own phoenix identity and 羽族 association.
