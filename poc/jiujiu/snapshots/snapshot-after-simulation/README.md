# Snapshot after final Phase 0 attempt

This snapshot captures the final TEST_SCAFFOLD state after the last supported `rehearse-arc` attempt and before any `project-all`, `seal`, `promote`, or `render` success.

Key facts:

- accepted/current chapter remains 0 and no prose chapter exists;
- `project-all` was never entered because the final rehearsal remained `ready_for_detail=false`;
- no Character Agent proposal, arbitration, or canonical memory file exists;
- final rehearsal input exposes the fox private mark/resource only to 九尾狐; 九九 and 凤凰 observation packets do not contain it;
- the source input SHA-256 remains unchanged;
- current rebase `source_root` equals `archive_root`;
- no pending usage call remains in the final snapshot;
- all generated story material remains TEST_SCAFFOLD.
