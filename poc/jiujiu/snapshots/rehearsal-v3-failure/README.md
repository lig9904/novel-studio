# V3 rehearsal failure snapshot

This snapshot preserves the first valid DeepSeek Flash World Arbiter report after physical resources and the V3 execution policy were enabled.

The report is speculative and `ready_for_detail=false`. It confirms that autonomy, knowledge isolation, shape constraints, and non-Canon labeling can remain intact, but rejects detailed planning because several soft-outline operations still refer to absent actors, a non-entity fox mark, exactly three paper sheets, or a non-actor boundary unit performing an in-world signature.

The subsequent direct `project-all` attempt was rejected before any model call because the current rehearsal was not ready. No gate was bypassed.
